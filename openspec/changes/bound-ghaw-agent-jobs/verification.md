# Verification: bound-ghaw-agent-jobs

OpenSpec-Verify: PASS
Verification-Method: manual semantic review of proposal, design, delta scenario, source and generated workflow diff; strict gh-aw compilation and validation; parsed YAML comparison of job permissions and timeouts; focused regression tests; full platform test groups; strict OpenSpec CLI validation
Automated-Checks-Evidence: automated-checks.json

## Semantic review

The three canonical Markdown sources each declare a 30-minute `jobs.agent` deadline and preserve their agentic execution-step budgets of 8, 10 and 10 minutes. Strict gh-aw v0.88.8 compilation produced the corresponding `jobs.agent.timeout-minutes: 30` in all three generated locks. The locks were generated by the compiler, not hand-edited. The generated agent steps still have 8, 10 and 10-minute timeouts, and the safe-output jobs retain their 45-minute deadlines.

The compiler migration updates generated setup actions and runtime images. The setup action is pinned to the immutable commit `69763a0390c8438e006d545f1faef1fab742719d`, confirmed as the commit behind tag `v0.88.8`; all generated `uses:` references remain full 40-character SHAs. The generated MCP gateway is v0.4.18 pinned by digest. The compiler replaced the obsolete v0.85.4 entry in `.github/aw/actions-lock.json` with the new release action SHA. Parsed old and new YAML showed the same six jobs and unchanged permissions on every job for all three workflows. The compiler added an optional `GH_AW_DEFAULT_OTLP_HEADERS` secret reference; the existing Codex key references and `allowed-repos: public` guard remain. The source keeps the same explicit gateway mount-root declaration and does not enable private-to-public flows. Documentation and regression checks describe the actual generated runtime.

The accepted delta requires whole-job deadlines independent of the agentic step deadline; the implementation and regression satisfy both. The 30-minute job budget leaves setup and cleanup time beyond the 8–10-minute agent step. No live GitHub Actions run of this branch was performed before publication; PR checks are the remote execution gate.

## Checks actually run

- `gh aw compile process-issue-triage weekly-process-backlog-review architecture-health-review --strict --validate --action-mode release --action-tag 69763a0390c8438e006d545f1faef1fab742719d`: passed for all three sources; two existing concurrency discriminator warnings remain.
- `python3 -m unittest tests.test_agentic_workflows`: 14 tests passed.
- Parsed all three generated YAML files: agent job deadlines are 30 minutes; agentic steps are 8, 10 and 10 minutes; every old/new job permission map is identical.
- `python3 -m compileall -q template/scripts scripts`: passed.
- `python3 scripts/managed_projects.py validate`: passed (3 managed projects).
- `python3 scripts/run_test_groups.py --all`: 13 groups passed, no failed groups, 1230 declared and discovered tests on the initial base; after reconciling with newer `origin/main`, all 13 groups passed again with 1232 declared and discovered tests.
- `python3 template/scripts/openspec_lifecycle.py check`: passed before task completion. After all task boxes were checked, it reported the expected active-change lifecycle debt; archive is the required resolution.
- `openspec validate bound-ghaw-agent-jobs --strict --no-interactive`: passed.
- `git diff --check`: passed.
- After the normal-history merge of `origin/main`, `python3 scripts/validate_agentic_workflows.py`, compileall and managed project registry validation passed again.
