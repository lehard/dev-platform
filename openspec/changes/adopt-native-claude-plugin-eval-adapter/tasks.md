- [x] Reconfirm the current supported Claude Code release containing `claude plugin eval` and record exact version/provenance. Confirmed against the real published npm package: the locally installed CLI (2.1.119) has no `eval` subcommand under `claude plugin`; `@anthropic-ai/claude-code@2.1.269`..`2.1.273` (npm registry, current latest at authoring time) do expose `claude plugin eval`. `MIN_NATIVE_VERSION = (2, 1, 269)` in `template/scripts/capability_evals.py` records this gate.
- [x] Inspect `claude plugin eval --help`, suite format, JSON output, scoring/status semantics, exit codes, runtime/credential requirements and cancellation/failure behavior. Ran `claude plugin eval --help` and a real bounded single-case run (`--runs 1 --max-cost-usd 0.20 --json`) against `@anthropic-ai/claude-code@2.1.273`. Findings: suite format is `<eval dir>/<case>/prompt.md` (+ `graders/*.md`) or `case.yaml`; `--json` writes `{schemaVersion, claudeVersion, partial, partialReason, cases:[{name, promptMarkdown, arms:{with:[{passed, error, ...}]}}], aggregates}`; exit 0 = all cases at/above `--threshold`, exit 1 = below threshold, exit 2 = aborted/partial (cost ceiling **or** a fatal runtime error such as auth failure — exit code alone does not distinguish these). Runtime/credential requirement discovered directly: the eval spawns a full `claude` child process on its own credential; in this host-bridged desktop session that child could not authenticate (`partial: true, partialReason: "auth_failed"`), so the adapter must treat native auth failure as `blocked/unavailable`, not as evidence.
- [x] Map native output to the existing provider-neutral eval schema; explicitly mark fields that cannot be mapped truthfully. Implemented in `run_claude_native` (`template/scripts/capability_evals.py`): per-run `passed`/`error` maps to `triggered`/`not-triggered`/`timeout`/`runtime-error`/`blocked/unavailable`/`unknown` and reuses the existing `_case_result` aggregation unchanged. Native `promptMarkdown`, `tracePath`, and the HTML report are read only to verify integrity (prompt hash) and are never copied into the canonical report. Native ablation (with/without) scores are not yet mapped to `quality_comparisons`; those stay `not-verified`/`None` for live Claude runs — an explicitly recorded boundary, not a silent gap.
- [x] Implement the smallest Claude adapter only if the native contract is sufficient; otherwise stop with a documented blocker. The native contract is sufficient (real JSON output carries pass/fail plus error text). `run_claude_native` + `probe_claude_native` in `template/scripts/capability_evals.py` bridge `claude plugin eval` without a nested `claude -p` runner or bespoke stream/event parser.
- [x] Route capability-management live Claude eval decisions through the native adapter when available. `capability_manager.py`'s existing `--runtime claude` plumbing (`update`/`evaluate`/`eval-decision`) now threads a new `--target`/`--claude-bin`/`--max-cost-usd`/`--timeout-seconds` through to `evals.run_fixture`; `decision_for` returns `run` for `runtime="claude"` on a material/explicit change, matching the `fixture` runtime's existing shape.
- [x] Keep the deterministic fixture path unchanged for keyless CI and unsupported-runtime testing. `run_fixture`'s `fixture`/`codex` branch is untouched (only refactored to share `_compose_report`/`_summarize` with the new claude branch); `tests/test_capability_manager.py`'s existing fixture/codex tests pass unmodified.
- [x] Verify unavailable/incompatible Claude runtime returns actionable `blocked/unavailable` without breaking ordinary capability lifecycle. Covered by `test_claude_native_adapter_is_blocked_unavailable_without_binary`, `test_claude_native_adapter_is_blocked_unavailable_below_minimum_version`, and `test_claude_native_adapter_reports_blocked_unavailable_on_auth_failure` in `tests/test_capability_manager.py`.
- [x] Verify Claude-specific report structures do not leak into canonical core records and that full HTML/transcripts are not retained as platform state. `run_claude_native` never reads `--report`/HTML output and discards `tracePath`/`promptMarkdown` after hash verification; `test_claude_native_adapter_maps_native_json_into_provider_neutral_report` asserts `"prompt"` is absent from a result record, matching the existing fixture-path invariant.
- [x] Verify no nested `claude -p` runner or bespoke event parser is introduced when the native surface covers the required execution contract. `run_claude_native` shells out to `claude plugin eval ... --json <path>` and parses the resulting JSON file only; it never spawns `claude -p` or parses a message/event stream.
- [x] Run relevant capability, routing, containment and lifecycle regression checks plus semantic OpenSpec verification. See `verification.md`.
