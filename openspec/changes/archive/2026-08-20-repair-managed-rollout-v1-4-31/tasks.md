# Tasks

## 1. Diagnose and specify

- [x] 1.1 Reproduce the v1.4.31 Cuby guarded-recopy failure and Planner Agent Lab CI permission failure from controlled fixtures/log evidence.
- [x] 1.2 Confirm ownership boundaries and update this change before implementation if current rollout code requires a different compatible insertion point.

## 2. Implement safe rollout repair

- [x] 2.1 Make the marked task-intake migration compatible with guarded protected-path comparison without accepting unrelated project-owned changes.
- [x] 2.2 Make shared-workspace permission enforcement distinguish supported local collaboration topology from GitHub-hosted CI.
- [x] 2.3 Preserve current Copier conflict handling and project-owned file protections.

## 3. Verify and operate

- [x] 3.1 Add regression tests for Cuby/Jara agent-rule fixtures, unexpected protected mutation, idempotence and CI/local permission classification.
- [x] 3.2 Run relevant rollout/doctor/template/upgrade checks and strict OpenSpec validation with a truthful receipt.
- [x] 3.3 Prepare patch release `v1.4.32`; its post-merge GitHub publication workflow creates the immutable tag and dispatches the exact-tag managed rollout without auto-merging downstream PRs. Observe and record those outcomes after merge.
