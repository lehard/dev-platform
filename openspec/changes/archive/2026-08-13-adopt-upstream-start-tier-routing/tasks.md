- [~] Wait for and reconcile the completed truthful execution provenance contract from `development-backlog#5`: **not resolved at implementation time** — `development-backlog#5` remains open (its own task 6.11 Codex leg is blocked on provider credits until 2026-08-18; its section 7 verify/archive/release has not started; its worktree/branch `agent/adopt-gh-aw-process-automation` is still active). Per explicit user authorization this change proceeds without waiting. To avoid creating a second telemetry/state machine ahead of #5 landing, this change extends the existing `.claude/model-routing/<change>.json` `Route` record additively (new `start_tier`/`freshness` fields alongside the unchanged provider/profile/execution fields) instead of inventing new provenance plumbing, so reconciling with whatever participant/supervisor schema #5 eventually lands remains a bounded, additive future step rather than a rewrite.
- [x] Reconcile current `model-routing` accepted spec, central/generated guidance, routing scripts/config and managed-task authoring package shape.
- [x] Define the provider-neutral R1/R2/R3 start-tier policy with R2 default and R1 production-disabled. Implemented in `template/scripts/start_tier_routing.py` (`recommend_start_tier`, `validate_routing_receipt`, `tier_to_profile`); `R1` is mapped in the rubric but `recommend_start_tier` never returns it and `tier_to_profile("R1")` refuses execution.
- [x] Add authoring-time routing receipt after bounded targeted repo inspection and expose `[R2]`/`[R3]` in managed Issue titles. `managed_task.py create --strong-trigger <category>` records the receipt and prefixes the created Issue title; `--strong-trigger` is validated against the bounded hard-trigger allow-list.
- [x] Separate start tier, effort hint and assurance in configuration/evidence without persisting concrete model IDs in managed task contracts. The routing receipt (`recommended_start_tier`, `rubric_version`, `task_family`, `routing_confidence`, `assurance`, `effort_hint`, `strong_trigger`) is provider-neutral; concrete models stay in `.dev-platform.toml`'s `[model_routing]`.
- [x] Replace mandatory strong-parent routing preflight with execution-time freshness confirm/escalate semantics. `model_routing.py prepare()`/`route-codex`/`route-claude` accept an optional `--profile`; omitting it confirms the authored tier (mapped via `tier_to_profile`) instead of requiring a fresh full assessment. `escalate()` now marks `freshness="escalated"` and is the freshness-escalate path.
- [x] Preserve provider-local direct/delegated execution, containment and truthful fallback/escalation evidence under the new entrypoint model. Containment (`delegation_containment.py`, `delegated_write_guard.py`), the routing gate (`dogfood_task.py require_routing_gate`) and existing escalation/fallback semantics are unchanged; only tier derivation and the mandatory-strong-parent framing changed.
- [x] Update central `AGENTS.md`, `template/AGENTS.md.jinja`, routing engineering docs/help text and any provider adapters so strong-parent-first is no longer stated as mandatory. Updated `AGENTS.md`, `template/AGENTS.md.jinja`, `docs/engineering/model-routing.md` (new "Start-tier authoring" / "Execution-time freshness check" sections) and `docs/engineering/agent-workflow.md`.
- [x] Add regression tests for R2 default, bounded R3 triggers, high-assurance R2, title/receipt authoring, direct balanced start, freshness escalation and model-mapping replacement. `tests/test_start_tier_routing.py` (new, 13 tests), plus additions to `tests/test_model_routing.py`, `tests/test_managed_task.py` and `tests/test_central_dogfood_lifecycle.py`. Model-mapping replacement was already covered by existing `test_dogfood_standard_dispatch_records_terra_and_launches_executor`/`test_dogfood_complex_dispatch_remains_on_sol`, unaffected by tier derivation since it only changes which profile is *selected*, not how a profile maps to a model.
- [x] Run semantic OpenSpec verification and the risk-proportional relevant platform checks before archive/publication. See `verification.md`.
