# Tasks

- [x] Advance the pinned gh-aw compiler to a version that emits agent-job timeouts; preserve strict compilation, pin the generated setup action by SHA, and review changed runtime/action/secret manifests and permissions.
- [x] Add bounded agent-job timeouts and required explicit bash declarations in the three canonical sources; regenerate all three locks with the new pin.
- [x] Add regression coverage for compiled agent-job deadlines and run focused checks.
- [x] Perform semantic OpenSpec verification and record actual evidence before archive and publication.
