# Design

Use the same current-worktree/root configuration reader already used by friction routing. Keep the operator-owned promotion repository as the destination. Exercise CLI `promote --dry-run` with a real local event and operator fixture so the regression reaches the command parser.
