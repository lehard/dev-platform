# Verification

OpenSpec-Verify: PASS
Verification-Method: Equivalent semantic review because no `/opsx:verify` tool integration is available in this environment; reviewed the proposal, design, delta specs, implementation, and checks for outcome fidelity, completeness, correctness, and coherence against lehard/development-backlog#125's acceptance criteria.
Automated-Checks-Evidence: automated-checks.json

## Semantic review

- **Outcome fidelity:** `scripts/public_distribution.py`'s `CANONICAL_PRODUCT_REPOSITORIES` now contains only `lehard/dev-platform`; the companion Development Backlog repository is no longer canonical product identity, matching proposal "What Changes" bullet 1 and the accepted-spec delta "Public product identity excludes operator integrations". The built-in `COMPATIBILITY_MARKERS` split-literal denylist (Jara/Planner-Agent-Lab/Cuby category markers) was removed entirely from shipped source and replaced by an external one-shot cutover-policy file (`load_cutover_policy`, `--cutover-policy` CLI flag on `audit`/`snapshot`), matching design.md decisions 3/4/5/6 and the accepted-spec delta's "One-shot cutover deny data is external to the shipped product" / "External cutover policy is explicit and fail-closed" requirements.
- **Completeness:** every current-candidate literal reference to `lehard/development-backlog` outside the canonical-identity mechanism itself was inventoried (14 files) and replaced with a synthetic `example-org/development-backlog` identity, or (for the one active-change `proposal.md` self-reference and the accepted `project-factory` spec sentence) reworded/delta-modified to avoid the literal owner/repo shape. `openspec/changes/<active>/.managed-task.json`'s own real source-issue provenance is excluded from the candidate set (`EXCLUDED_PARTS` gained the `.managed-task.json` filename) because it is task/operator tracking state analogous to the already-excluded `.managed-task-state.json`, not product source, and every archived change's own copy was already excluded via the `openspec/changes/archive` exclusion. The `project-factory` accepted-spec requirement "Project Factory renders a generic project without operator coupling by default" is replaced via a `## MODIFIED Requirements` delta so the archived text no longer names the literal repository. The operator handoff checklist (task 4) was added to `docs/public-cutover.md`, naming exactly the `[development_backlog]`, `[rollout] registry_path`/`bot_login`, and `[rollout.legacy_harness_migrations]` fields plus their real validation points (`scripts/operator_doctor.py`, `scripts/managed_task.py`, `scripts/rollout_project.py`), without inventing or committing any real operator value.
- **Correctness:** see "Executed evidence" below; the full test suite passed both before and after this change's edits, the new/rewritten `tests/test_public_distribution.py` cases (cutover-policy validity, missing/malformed fail-closed, marker/prohibited-repository detection, receipt redaction, policy-file self-exclusion) passed, and `openspec validate finalize-public-snapshot-identity-boundary --strict` passed.
- **Coherence:** the external cutover-policy schema stayed the smallest useful shape (version + two label-keyed dicts) per design.md decision 4 ("data, not a plugin framework"); every diagnostic and receipt field that could carry a private deny value instead carries only its label/basename/version/digest (design.md decision 5), verified by dedicated tests asserting the raw synthetic deny strings used in those tests never appear in `json.dumps(receipt)`. `CANONICAL_PRODUCT_REPOSITORIES` is exactly `{CANONICAL_PRODUCT_REPOSITORY}` (a regression test asserts this and that `COMPATIBILITY_MARKERS` no longer exists as a module attribute), so there is no way to accidentally re-widen the allowlist without a visible, reviewed source change.

## Executed evidence (pre-archive)

- `python3 -m compileall -q template/scripts scripts tests` — clean.
- `python3 -m unittest tests.test_public_distribution -v` — 17 tests, all passed (includes 6 new cutover-policy tests and 1 new no-built-in-denylist regression test).
- `python3 scripts/run_test_groups.py --all` — 908 declared == 908 discovered tests, 13 groups, all successful.
- `python3 scripts/select_checks.py --base origin/main --execute` — fell back to the full suite (broad diff across docs/tests/OpenSpec); two groups (`delegated_write_guard`, `publication_recovery_cli`) failed under the 13-way parallel run with timing-sensitive symptoms ("first writer never reached its assigned worktree"; a 0.3s bounded-deadline helper test). Re-ran both modules in isolation immediately afterward (`python3 -m unittest tests.test_delegated_write_guard` and `tests.test_publication_recovery_cli`): both fully green (49 and 17 tests respectively). Classified `pre-existing`/`unrelated`: neither touched file is part of this change's diff, and the symptoms (process-launch race, CPU-contention-sensitive short deadline) are consistent with parallel-run flakiness, not a regression.
- `openspec validate finalize-public-snapshot-identity-boundary --strict` — `Change 'finalize-public-snapshot-identity-boundary' is valid`.
- `python3 template/scripts/openspec_lifecycle.py check` — `OpenSpec lifecycle hygiene: OK`.
- `python3 scripts/check_docs_links.py` — `no problems found`.
- `python3 scripts/public_distribution.py audit` — one finding: `openspec/specs/project-factory/spec.md: lehard/development-backlog` (the accepted-spec sentence this change's `MODIFIED Requirements` delta replaces at archive time; expected until archive).
- `python3 scripts/public_distribution.py audit --cutover-policy <empty policy>` — same single expected finding; proves the `--cutover-policy` flag wiring end-to-end against the real tree (no other private deny data currently exists in-tree to demonstrate against).
- `python3 scripts/public_distribution.py history-audit` — 0 findings across 7832 enumerated objects / 2584 examined blobs.
- `python3 tests/public_distribution_snapshot_smoke.py` — failed pre-archive with exactly the one expected finding above (the still-unarchived accepted-spec sentence); see "Post-archive confirmation" below for the definitive run.

## Post-archive confirmation

`python3 template/scripts/openspec_lifecycle.py archive finalize-public-snapshot-identity-boundary` ran `select_checks.py --base origin/main --execute` internally first (`python3 -m compileall` then `python3 scripts/run_test_groups.py --all`, 245.966s, success), then `openspec validate --strict`, `openspec archive --yes` (applied `+7 added, ~1 modified` across `platform-config`/`project-factory`/`public-distribution` accepted specs), then `openspec validate --all --strict` (31/31 passed). Archived as `openspec/changes/archive/2026-09-19-finalize-public-snapshot-identity-boundary/`.

Immediately after, `python3 scripts/public_distribution.py audit` on the archived tree was clean (0 findings in every category, including `openspec/specs/project-factory/spec.md` no longer containing the literal `lehard/development-backlog` string), confirming the pre-archive prediction. `python3 scripts/public_distribution.py history-audit`, `python3 scripts/check_docs_links.py`, `python3 template/scripts/openspec_lifecycle.py check`, and `openspec validate --all --strict --no-interactive` were all clean/OK. `python3 scripts/managed_projects.py --registry tests/fixtures/managed-projects.json validate` reported `OK (1 managed, 1 candidate, 1 excluded)` (the real external operator registry on this machine has no `rollout.registry_path` configured -- a pre-existing environment gap, unrelated to this change, matching the precedent in `openspec/changes/archive/2026-09-19-complete-public-snapshot-source-boundary/verification.md`).

`python3 tests/public_distribution_snapshot_smoke.py` (extracts the real candidate set, gives it fresh single-commit Git history, then runs `compileall`, `run_test_groups.py --all`, and `openspec_lifecycle.py check` from inside it) needed three attempts, all against the identical archived source revision, because of confirmed environmental contention rather than any packaging or code defect:

1. **First attempt:** failed with `delegated_write_guard` ("first writer never reached its assigned worktree") and `publication_recovery_cli` (`test_expired_helper_fails_with_process_identity_and_retained_output`, a 0.3s bounded-deadline helper test) both failing. At the time, `ps aux` showed two other, unrelated worktrees on this same machine (`add-context-learning-loop`, `add-add-intents-openspec-pipeline`) simultaneously running their own full `select_checks.py`/`run_test_groups.py --all`, actively competing for CPU.
2. **Second attempt (immediately after):** `delegated_write_guard` passed clean; only `publication_recovery_cli`'s same single 0.3s-deadline test failed again with the same symptom (helper process SIGKILLed before it could flush partial output).
3. **Isolated confirmation:** `python3 -m unittest tests.test_publication_recovery_cli -v` run standalone (no concurrent load) passed all 17 tests cleanly, both immediately after the pre-archive `select_checks.py --execute` run and again after the second smoke attempt.

Classified `pre-existing`/`unrelated` per the openspec-workflow.md failure-classification guidance: `tests/test_publication_recovery_cli.py` is not part of this change's diff, the failure is a single hardcoded 300ms deadline assertion inherently sensitive to CPU scheduling latency, it reproduced only under demonstrated concurrent load from other agents' worktrees and never in isolation (three isolated runs, three passes), and it is unrelated to `scripts/public_distribution.py` or any file this change touches. Per "Other agents' state is off limits" this task does not touch, wait on, or otherwise interfere with those other worktrees' processes; a fourth full ~30-minute smoke retry was not run, since the isolated evidence is already reproducible and conclusive. The snapshot's own deterministic content (candidate file list, path completeness, `openspec validate` from inside the extracted tree) was proven clean and unaffected across all three attempts -- only this one pre-existing flaky test selection differed run to run.

Exact revision: the archived commit is the definitive final one for this change; `python3 scripts/public_distribution.py audit` (0 findings) and `python3 scripts/public_distribution.py history-audit` (0 findings across 7832 enumerated objects / 2584 examined blobs) were both run against it.

## Explicitly out of scope (reported, not performed)

Per proposal "Impact" and tasks.md 5.5, this change performs no GitHub repository create/rename/visibility cutover and no downstream repository migration. The real operator handoff (populating `[development_backlog]`, `[rollout] registry_path`/`bot_login`, and any `[rollout.legacy_harness_migrations]` values in this operator's actual external TOML) remains a separate action for the maintainer, documented in `docs/public-cutover.md`'s new "Operator handoff checklist" section; no real value for any of those fields is committed by this change.
