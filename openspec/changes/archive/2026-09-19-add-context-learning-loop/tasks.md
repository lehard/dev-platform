## 1. Context-gap evidence model

- [x] 1.1 Extend the existing friction event model with an optional `context-gap` classification and bounded project-context concern/destination metadata.
- [x] 1.2 Keep new fields optional so existing non-context friction remains compatible.
- [x] 1.3 Reuse existing sanitization and ensure raw transcripts, secrets, credentials, and sensitive source material are not promoted as context evidence.

## 2. Deduplication and retrospective

- [x] 2.1 Ensure root-cause fingerprinting for context gaps remains independent of model/provider identity.
- [x] 2.2 Record recurrence as evidence strength while explicitly preventing count-based automatic canonical writes or managed-task creation.
- [x] 2.3 Update post-task retrospective guidance so a relevant user correction/repeated semantic failure can be classified as a context gap without forcing every agent error into that category.

## 3. Process Health Review and promotion

- [x] 3.1 Extend review classification/grouping to distinguish context gaps from tooling/process friction and name the likely context concern/destination when evidence supports it.
- [x] 3.2 Preserve observation/evidence/hypothesis/proposal separation and read-only review semantics.
- [x] 3.3 Route an explicitly accepted context improvement through the existing managed-task/OpenSpec intake with linked process evidence where supported.

## 4. Regression evidence and delivery

- [x] 4.1 Cover representative user-correction context-gap recording and sanitization.
- [x] 4.2 Cover equivalent repeated context gaps across different provider/model identities converging on one evidence identity.
- [x] 4.3 Cover three repeated observations producing no automatic context edit or managed task.
- [x] 4.4 Cover a tooling/process failure remaining outside context-gap classification.
- [x] 4.5 Run relevant friction/process-review tests and semantic OpenSpec verification, record truthful evidence, archive, and publish through the normal lifecycle.
