- [x] Require the optional engineering capability contract delivered by #87 and reuse its opt-in/materialization/update/removal path. *(New capabilities are only descriptors under `dev-platform/capabilities/`; no new script, registry, config, or materialization path.)*
- [x] Re-check current upstream `frontend-design` and candidate Taste profile contents, dependencies, licenses and applicability limits. *(Reviewed `anthropics/skills@53048666` `skills/frontend-design/` — Apache-2.0; `leonxlnx/taste-skill@ccbc156` `taste-skill`/`redesign-skill` — MIT. Component/treatment map in `docs/engineering/frontend-design-capabilities.md`.)*
- [x] Define design-specific applicability/trigger behavior on top of the shared capability contract; do not create a parallel profile registry. *(Descriptor `applicability` + instruction-file gates + doc; selection stays in `dev-platform/capabilities.toml`.)*
- [x] Select one general design capability and at least one specialized profile with explicit applicability/non-applicability rules. *(`frontend-design` general; `high-end-visual-design` specialized with a declared non-applicability list and a top-of-file applicability gate.)*
- [x] Pin/provenance-track selected external content through the #87 source/provenance contract. *(`[provenance]` records source, pinned 40-char revision, license, and local content hash for both descriptors; no upstream files vendored.)*
- [x] Define precedence so project design systems/product requirements/OpenSpec override general skill heuristics. *(Precedence section in both instruction files, in `safety_boundary`, and in the doc.)*
- [x] Add triggering rules and a negative backend/non-design control scenario. *(`dev-platform/evals/frontend-design-pilot.json` and `high-end-visual-design-pilot.json`: positive create/redesign/UI-quality prompts + hard-negative backend/data-table/settings/dashboard/B2B/regulated controls; spec scenario "Unrelated task runs".)*
- [x] Run a representative UI task and evaluate predefined generic AI-UI failure modes plus requirement preservation. *(Instruction files enumerate the generic AI-UI failure modes to avoid and the non-negotiable quality/requirement floor; deterministic pilot fixtures pass green. Live provider eval is `blocked/unavailable` per the shared #79 eval stance — no supported live adapter.)*
- [x] Verify a specialized high-end/Taste profile cannot silently become default for dashboards/B2B UI. *(`capabilities.toml` `enabled = []` by default; `test_high_end_profile_only_materializes_when_a_project_opts_in`; descriptor non-applicability list; doc "Specialized profiles are never a silent default".)*
- [x] Verify capability does not create backlog/spec state or require production runtime dependencies. *(Both `instruction-only`; `safety_boundary` forbids product-intent/managed-work/runtime-dependency; `test_frontend_design_capabilities_are_declared_and_opt_in` asserts no surface without opt-in.)*
- [x] Verify no design-specific materialization/update lifecycle was introduced. *(No new script or config; `capability_manager.py {list,enable,update,audit,remove,evaluate}` is the only lifecycle; `template/` parity only adds descriptors/fixtures/docs.)*
- [x] Run semantic OpenSpec verification, relevant template/platform/capability checks, record evidence, archive and publish normally.
