# Tasks

## 1. Contract and selection

- [x] Add the default-off generic Copier selection and update accepted configuration, rollout, and instruction contracts.
- [x] Extend the private-registry schema/matrix and rollout CLI without exposing operator inventory or identity in public source.

## 2. Rendering and migration

- [x] Render environment-based operator opt-in for selected projects while retaining the legacy path input.
- [x] Migrate only the deterministic platform-owned table during rollout and fail closed on conflicting project-owned operator configuration.
- [x] Make all rendered operator-aware guidance recognize the new selection, including task-intake and ChatGPT parity references.

## 3. Operator rollout data

- [x] Mark the three requested private-registry projects as operator-integrated and validate the registry.

## 4. Verify and deliver

- [x] Exercise portable, enabled, legacy, bootstrap, registry/matrix, rollout, and intake-semantic tests.
- [x] Inspect the Development Backlog Project view and record either verification or one exact manual UI instruction: in Development Backlog Project #1, set the primary Requirements view filter to exclude issues labeled `type:internal-change` (for example `Label` → `is not` → `type:internal-change`), while keeping a separate internal-child view available.
- [x] Obtain independent review, resolve findings, and record truthful verification for the reviewed implementation.
