# Verification

OpenSpec-Verify: PASS
Verification-Method: Equivalent semantic review because installed OpenSpec CLI 1.13.0 has no `verify` command; reviewed the proposal, design, delta specs, implementation, and checks for outcome fidelity, completeness, correctness, and coherence.
Automated-Checks-Evidence: automated-checks.json

## Semantic review

- **Outcome fidelity:** the public-distribution candidate policy now retains `tests/`, `openspec/specs/`, and current OpenSpec lifecycle configuration instead of blanket-excluding them, with only `openspec/changes/archive/` (maintenance-only history) explicitly excluded; audit/snapshot share one deterministic candidate set as before. A new `missing_required_paths` check proves packaged README/CI never references an omitted repository path, and a new extracted-snapshot smoke (`tests/public_distribution_snapshot_smoke.py`) proves the packaged tarball, given fresh Git history, is self-contained and developable from its first commit (compiles, passes the full test suite, and passes OpenSpec lifecycle hygiene).
- **Completeness:** `scripts/rollout_project.py` no longer contains any private-project name, message, or fingerprint. All `JARA_*`/`PLANNER_*` identifiers, comments, and embedded messages were replaced with generic `LEGACY_MERGE_HARNESS_*`/`LEGACY_PUBLISH_HARNESS_*` names. The two known-live shims (the legacy merge-harness and legacy publish-harness exact-head migrations, evidenced as still active for a real downstream project by the already-archived `2026-08-20-activate-exact-head-harness-migration` and `2026-08-20-adapt-jara-exact-head-regression-tests` changes) are not obsolete, so per design.md decision 6/7 their real repository identity and reviewed SHA-256 fingerprints were externalized to operator-owned configuration (`[rollout.legacy_harness_migrations]`, loaded by `apply_operator_legacy_harness_continuity()` from `main()`, never at import time) rather than deleted outright; the public candidate ships only the generic mechanism with synthetic `example-org/*` identities and `None` fingerprints, so it matches nothing and fails closed with the existing "unrecognized harness shape" diagnostic until an operator configures it. `scripts/public_distribution.py` gained `COMPATIBILITY_MARKERS` (three known private-project code-name markers, matched independent of `owner/repo` shape) and `missing_required_paths`/`referenced_source_paths` (README/CI path-completeness). All test/doc references to the three private projects were replaced with synthetic `example-org/*` fixtures or generic "legacy"/"downstream" wording; accepted OpenSpec spec scenario titles/text in `managed-task-intake`, `managed-rollout`, `platform-rollout`, and `project-factory` were genericized the same way, including this change's own delta spec (which must not itself leak the names it exists to remove, since accepted specs — unlike this change's own `proposal.md`/`design.md`/`tasks.md` — are not excluded from the snapshot).
- **Correctness:** a full run of `python3 scripts/run_test_groups.py --all` (896 declared == 896 discovered, 0 duplicates) passed across all 13 groups. `tests/test_rollout_recopy.py` (37 tests covering the exact-head migration mechanism, now exercised through the renamed generic constants) passed unchanged in behavior. `tests/test_public_distribution.py` (12 tests, 6 new) covers: tests/specs staying in the candidate set, `openspec/changes/archive/` exclusion, a compatibility-marker finding without `owner/repo` shape, missing-required-path detection from both README and CI workflow references, and that canonical/synthetic identities do not themselves trip the sanitizer. `tests/rollout_recopy_smoke.py` and `tests/upgrade_smoke.py --profile light` (real `copier` renders) passed. `tests/public_distribution_snapshot_smoke.py` was run against the real repository: before this change's own `proposal.md`/`design.md`/`tasks.md` were archived it correctly failed on exactly those three files (the change's own record of the private names it removes) and on nothing else; after archive (below) it passed cleanly end-to-end, including a fresh single-commit `run_test_groups.py --all` inside the extracted tarball.
- **Coherence:** the canonical-identity allowlist was narrowed rather than widened to cover the new candidate surface: only `lehard/dev-platform` and the real, publicly-documented `lehard/development-backlog` companion repository are accepted; every other `lehard/*` string found in now-included test fixtures was a synthetic placeholder and was renamed to a non-`lehard` synthetic owner (`example-org/*`) instead of being allow-listed. `COMPATIBILITY_MARKERS` dict keys and regex construction were built from split literals and generic category labels specifically so `scripts/public_distribution.py`'s own committed source never trips its own detector (verified: a naive first pass using the real names as literal dict keys did self-trip, e.g. the key `"jara_fin"` case-insensitively matches its own `Jara[ _]?Fin` pattern; keys were renamed to `legacy_merge_partner`/`legacy_publish_partner`/`legacy_ignore_partner`).

## Executed evidence

- `python3 -m compileall -q template/scripts scripts`
- `python3 scripts/run_test_groups.py --all` — 896 tests, 13 groups, all successful (`DEV_PLATFORM_TEST_JOBS=3`).
- `python3 -m unittest tests.test_public_distribution tests.test_rollout_recopy tests.test_managed_rollout tests.test_adopt_project tests.test_managed_intake_project_harness_isolation tests.test_rollout_diagnostic_envelope tests.test_rollout_failure_streak tests.test_rollout_preflight tests.test_managed_project_promotion tests.test_rollout_supersession` — 157 tests, all successful.
- `python3 scripts/managed_projects.py --registry tests/fixtures/managed-projects.json validate` — `OK (1 managed, 1 candidate, 1 excluded)`. The real external operator registry on this machine has no `rollout.registry_path` configured (a pre-existing environment gap, reproduced identically on a clean `main` checkout, unrelated to this change); validated against the committed fixture registry instead, matching the precedent in `openspec/changes/archive/2026-09-19-harden-public-distribution-operator-and-gitlab-boundaries/verification.md`.
- `python3 template/scripts/openspec_lifecycle.py check` — `OpenSpec lifecycle hygiene: OK`.
- `npx --yes @fission-ai/openspec@1.13.0 validate --all --strict --no-interactive` — `Totals: 31 passed, 0 failed`.
- `python3 tests/openspec_1_13_regression.py --openspec-command "npx --yes @fission-ai/openspec@1.13.0"` — passed.
- `python3 tests/rollout_recopy_smoke.py` — `Safe project-harness transition smoke passed via guarded-recopy.`
- `python3 tests/upgrade_smoke.py --profile light --publish-mode direct` — `Upgrade smoke passed: base=v1.4.38 profile=light publish=direct`.
- `python3 scripts/public_distribution.py history-audit` — 0 findings across 7745 enumerated objects / 2530 examined blobs.
- `python3 tests/public_distribution_snapshot_smoke.py` — pre-archive: failed with exactly the three expected self-referential findings in this change's own active `proposal.md`/`design.md`/`tasks.md` (compatibility markers naming the private projects this change removes); post-archive: passed (see below).

## Post-archive confirmation

Re-executed after `python3 template/scripts/openspec_lifecycle.py archive complete-public-snapshot-source-boundary` moved this change's `proposal.md`/`design.md`/`tasks.md` under the excluded `openspec/changes/archive/` subtree and merged the delta into `openspec/specs/public-distribution/spec.md`:

- `python3 scripts/public_distribution.py audit` — clean (no findings).
- `python3 tests/public_distribution_snapshot_smoke.py` — passed: snapshot built from the archived tree, extracted, given fresh single-commit Git history, and `compileall` + the full `run_test_groups.py --all` + `openspec_lifecycle.py check` all succeeded from inside the extracted, self-contained tree.

## Explicitly out of scope (reported, not performed)

Per proposal Impact and tasks.md 5.5, this change does not create, rename, or change the visibility of any GitHub repository, and does not migrate any downstream repository. Two items require a follow-up controlled action from the operator, not this task:

1. The real repository identity and reviewed SHA-256 fingerprints for the two live legacy-harness migrations, previously hard-coded in `scripts/rollout_project.py`, must be added to the operator's external operator TOML under `[rollout.legacy_harness_migrations]` (schema documented in `docs/operator-config.example.toml`) before the next real rollout targets those two downstream repositories, or their rollout will fail closed with an actionable "publication-safety compatibility blocker" diagnostic (safe, not silent) until configured.
2. The external operator registry used for cross-repository managed rollout (`[rollout].registry_path` in the operator's external TOML) is not currently configured on this machine/account; `scripts/managed_projects.py validate` and any real rollout dispatch depending on it will fail until an operator sets it. This is a pre-existing gap, reproduced identically on a clean `main` checkout, unrelated to and not introduced by this change.
